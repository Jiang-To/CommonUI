{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "process",
            "command": "dotnet",
            "args": [
                // Ask msbuild to generate full paths for file names.
                "build",
                "${workspaceFolder}/CommonUI.Server/CommonUI.Server.csproj"
            ]
        }
    ]
}